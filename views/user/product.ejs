<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Tour Packages</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" />
</head>
<body class="bg-white text-gray-800">

  <div class="max-w-7xl mx-auto px-4 py-8">
    <h2 class="text-3xl font-bold mb-8">Tour Packages</h2>

    <% if (products.length > 0) { %>
      <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-8">
        <% products.forEach((product, index) => { %>
          <div class="bg-white shadow-md border rounded-lg overflow-hidden hover:shadow-xl transition duration-300">
            <img src="<%= product.imageUrl[0] || '/images/default-product.jpg' %>" 
                 alt="<%= product.title %>" 
                 class="h-52 w-full object-cover" 
                 onerror="this.src='/images/default-product.jpg'"
            />

            <div class="p-4">
              <h3 class="text-lg font-semibold mb-1"><%= product.title %></h3>
              <p class="text-orange-600 font-bold text-sm">from <%= product.price %> €</p>

              <div class="text-xs text-gray-600 mt-1">
                <span class="mr-3"><i class="fas fa-star text-yellow-500"></i> <%= product.rating %> | <%= product.reviews %> reviews</span>
                <span><i class="far fa-clock"></i> <%= product.duration %> hrs</span>
              </div>

              <p class="mt-2 text-sm text-gray-700 line-clamp-2"><%= product.description %></p>

              <a href="/tour/<%= product._id %>" class="mt-3 inline-block text-orange-600 font-semibold text-sm hover:underline">Read More →</a>
            </div>
          </div>
        <% }); %>
      </div>
    <% } else { %>
      <p class="text-center text-gray-500 text-lg">No tour packages available at the moment.</p>
    <% } %>
  </div>

</body>
</html>
