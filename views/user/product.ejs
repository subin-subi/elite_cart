<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Product</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" />
</head>
<body class="bg-white text-gray-800">
  <%-include("../../views/partials/nav")%>
  <div class="max-w-7xl mx-auto px-4 py-8">
    <h2 class="text-3xl font-bold mb-8">Product</h2>

    <% if (products.length > 0) { %>
      <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-8">
        <% products.forEach((product, index) => { %>
          <div class="bg-white shadow-md border rounded-lg overflow-hidden hover:shadow-xl transition duration-300">
            <img src="<%= product.media.coverImage %>" 
                 alt="<%= product.title %>" 
                 class="h-52 w-full object-cover" 
                 onerror="this.src='/images/table1.jpg'"
            />

            <div class="p-4">
              <h3 class="text-lg font-semibold mb-1"><%= product.title %></h3>
              <p class="text-orange-600 font-bold text-sm">from <%= product.price %> €</p>

              <div class="text-xs text-gray-600 mt-1">
                <span class="mr-3"><i class="fas fa-star text-yellow-500"></i> <%= product.rating %> | <%= product.reviews %> reviews</span>
 
              </div>

              <p class="mt-2 text-sm text-gray-700 line-clamp-2"><%= product.description %></p>

              <a href="/productdetail/<%= product._id %>" 
                class="mt-4 inline-block bg-orange-600 text-black text-base font-semibold px-6 py-2 rounded-lg shadow-md hover:bg-orange-700 transition duration-300 ease-in-out">
               Buy Now →
             </a>
             
             
            </div>

            
          </div>
        <% }); %>
      </div>
    <% } else { %>
      <p class="text-center text-gray-500 text-lg">No product available at the moment.</p>
    <% } %>
  </div>

</body>
</html>
